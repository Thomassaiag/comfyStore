import React from "react";

const SectionTitle = ({ title }) => {
	return (
		<div className="align-item border-b border-base-300 pb-5">
			<h2 className="text-3xl font-medium trakcing-wider">{title}</h2>
		</div>
	);
};

export default SectionTitle;
